string  name              # motor name from config
float64 position          # rad   — target output shaft position
float64 velocity          # rad/s — speed limit (0 = use motor V_MAX)
float64 current_per_unit  # 0.0–1.0 — fraction of motor Imax (0 = use full Imax)
