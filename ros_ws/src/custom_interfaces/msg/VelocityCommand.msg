string  name              # motor name from config
float64 velocity          # rad/s  — target velocity
float64 current_limit     # A      — 0 = motor default (MAX_CURRENT_A)
float64 acceleration      # rad/s² — 0 = firmware default (20 rad/s²)
