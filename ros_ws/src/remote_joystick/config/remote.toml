# remote_joystick — remote machine config (server mode)
# Receives gamepad data from local client and publishes as sensor_msgs/Joy

hz = 20.0
debug = false
joy_topic = "joy"
mode = "server"

# Network interface to listen on (0.0.0.0 = all interfaces)
listen_host = "127.0.0.1"
listen_port = 5566
